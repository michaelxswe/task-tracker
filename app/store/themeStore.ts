// import { create } from "zustand";

// interface ThemeStore {
//   darkMode: boolean;
//   setDarkMode: () => void;
// }

// export const useThemeStore = create<ThemeStore>((set) => ({
//   darkMode: true,
//   setDarkMode: () => set((prev) => ({ darkMode: !prev.darkMode })),
// }));
